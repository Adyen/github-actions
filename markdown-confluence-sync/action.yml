name: Confluence Markdown sync
description: Sync Markdown files from the checked-out repository to existing Confluence pages.

inputs:
  banner:
    description: Optional Markdown warning banner injected at the top of the Confluence page.
    required: false

runs:
  using: node24
  main: dist/index.js
